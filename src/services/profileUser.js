export const handleMessageCheckInfo = (message) => {
   message.channel.send('myinfo is already!');
};

export const handleMessageCheckKPI = (message) => {
   message.channel.send('kpi is already!');
};

export const handleMessageCheckRank = (message) => {
   message.channel.send('ranking is already!');
};
